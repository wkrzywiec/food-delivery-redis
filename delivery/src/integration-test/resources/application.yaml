management:
  endpoints:
    web:
      exposure:
        include: '*'

spring:
  application:
    name: 'food-delivery_delivery'
  datasource:
    driver-class-name: io.wkrzywiec.fooddelivery.delivery.infra.repository.JsonbPostgreSQLDialect
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true